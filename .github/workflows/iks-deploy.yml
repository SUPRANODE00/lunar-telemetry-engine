name: Build and Deploy to IKS (Capsule Mesh Telemetry)

on:
  push:
    branches: [ "main", "feat/stateless-control-plane" ]

env:
  GITHUB_SHA: ${{ github.sha }}
  IBM_CLOUD_API_KEY: ${{ secrets.IBM_CLOUD_API_KEY }}
  IBM_CLOUD_REGION: us-south
  ICR_NAMESPACE: ${{ secrets.ICR_NAMESPACE }}
  REGISTRY_HOSTNAME: us.icr.io
  IMAGE_NAME: lunar-telemetry-engine
  IKS_CLUSTER: example-iks-cluster-name-or-id
  DEPLOYMENT_NAME: capsule-node-deployment
  PORT: 8080

jobs:
  setup-build-publish-deploy:
    name: Setup, Build, Publish, and Deploy Capsule Mesh
    runs-on: ubuntu-latest
    environment: production
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Install IBM Cloud CLI
      run: |
        curl -fsSL https://clis.cloud.ibm.com/install/linux | sh
        ibmcloud --version
        ibmcloud config --check-version=false
        ibmcloud plugin install -f kubernetes-service
        ibmcloud plugin install -f container-registry

    - name: Authenticate with IBM Cloud CLI
      run: |
    - name: Authenticate with IBM Cloud CLI
      run: |
        if [ -z "${IBM_CLOUD_API_KEY}" ]; then
          echo "Error: IBM_CLOUD_API_KEY secret is not set in GitHub repository settings."
          exit 1
        fi
    - name: Authenticate with IBM Cloud CLI
      run: |
        if [ -z "${IBM_CLOUD_API_KEY}" ]; then
          echo "Error: IBM_CLOUD_API_KEY secret is not set in GitHub repository settings."
          exit 1
        fi
        ibmcloud login --apikey "${IBM_CLOUD_API_KEY}" -r "${IBM_CLOUD_REGION}" -g default
        ibmcloud cr region-set "${IBM_CLOUD_REGION}"
        ibmcloud cr login
        ibmcloud cr region-set "${IBM_CLOUD_REGION}"
        ibmcloud cr login
        ibmcloud cr region-set "${IBM_CLOUD_REGION}"
        ibmcloud cr login

    - name: Build Docker Image with Telemetry Context
      run: |
        docker build -t "$REGISTRY_HOSTNAME"/"$ICR_NAMESPACE"/"$IMAGE_NAME":"$GITHUB_SHA" \
          --build-arg GITHUB_SHA="$GITHUB_SHA" \
          --build-arg GITHUB_REF="$GITHUB_REF" .

    - name: Push Docker Image to IBM Container Registry
      run: |
        docker push $REGISTRY_HOSTNAME/$ICR_NAMESPACE/$IMAGE_NAME:$GITHUB_SHA

    - name: Deploy Container to IKS Cluster with Health Probes
      run: |
        ibmcloud ks cluster config --cluster $IKS_CLUSTER
        kubectl config current-context
        kubectl create deployment $DEPLOYMENT_NAME \
          --image=$REGISTRY_HOSTNAME/$ICR_NAMESPACE/$IMAGE_NAME:$GITHUB_SHA \
          --dry-run=client -o yaml > deployment.yaml
        kubectl apply -f deployment.yaml
        kubectl rollout status deployment/$DEPLOYMENT_NAME
        kubectl create service loadbalancer $DEPLOYMENT_NAME \
          --tcp=8080:$PORT --dry-run=client -o yaml > service.yaml
        kubectl apply -f service.yaml
        kubectl get services -o wide
